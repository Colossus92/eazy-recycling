export type Truck = {
  licensePlate: string;
  model: string;
  brand: string;
};

export type Address = {
  streetName: string;
  buildingNumber: string;
  postalCode: string;
  city: string;
};

export type DeleteResponse = { success: boolean };
