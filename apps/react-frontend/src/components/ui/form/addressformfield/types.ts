import { FieldValues } from 'react-hook-form';

/**
 * Generic type variable for form values used across AddressFormField components
 * This allows all sub-components to work with any form structure
 */
export type TFieldValues = FieldValues;
